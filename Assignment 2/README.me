This program uses Jupyter notebook.  To run this program, simply load up a jupyter notebook file and run all of the methods.  letterLangId uses a letter bigram. wordLangId uses a word bigram.  both will create output files with respective names.